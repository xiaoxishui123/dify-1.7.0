# 语音合成和工作流常见问题故障排除汇总

## 🚨 常见问题快速索引

### 1. 语音合成节点报错
**错误**: `Reached maximum retries (3) for URL http://8.148.70.18:3006/api/v1/tts`
**解决**: TTS服务未启动
```bash
cd /home/dify/tts_gateway
./start.sh
```

### 2. 音频文件媒体丢失
**错误**: CapCut中显示"媒体丢失 Media Not Found"
**解决**: 音频URL格式问题，已在工作流中修复（.bin改为.mp3）

### 3. TTS工具参数配置错误
**错误**: `Value error, 不支持的音色类型: {{#start_1.tts_voice#|default(...)}}`
**解决**: 工具参数类型配置错误，模板变量未被正确解析
- 将 `voice_type` 参数类型改为 `constant`
- 将 `speed_ratio` 参数类型改为 `constant`

### 4. Dify工作流导入失败
**错误**: 点击"创建应用"报错"创建应用失败"
**解决**: 检查依赖插件和工具配置

---

## 🔧 快速修复命令

### 检查和重启TTS服务
```bash
# 检查TTS服务状态
ps aux | grep tts_gateway

# 重启TTS服务
cd /home/dify/tts_gateway && ./start.sh

# 测试TTS服务
curl http://localhost:3006/
```

### 检查CapCut API服务
```bash
# 测试CapCut API
curl http://8.148.70.18:9000/

# 检查端口占用
netstat -tlnp | grep :9000
```

### 验证网络连接
```bash
# 测试网络连接
ping 8.148.70.18
curl -I http://8.148.70.18:3006
curl -I http://8.148.70.18:9000
```

### 检查TTS工具配置
```bash
# 测试TTS API参数
curl -X POST http://8.148.70.18:3006/api/v1/tts \
  -H "Content-Type: application/json" \
  -d '{
    "text": "测试语音合成", 
    "voice_type": "zh_male_dongfanghaoran_moon_bigtts",
    "speed_ratio": "1.0",
    "audio_format": "mp3",
    "sample_rate": 24000
  }'
```

---

## 📋 导入工作流问题解决

### 导入前检查
1. **依赖插件** - 确保已安装：
   - `allenwriter/doubao_image:0.0.1`
   - `langgenius/siliconflow:0.0.20`
   - `langgenius/volcengine_maas:0.0.25`

2. **自定义工具** - TTS工具配置：
   - 工具名称：`tts_gateway3006`
   - API端点：`http://localhost:3006/api/v1/tts`

3. **文件检查** - 工作流文件：
   - 位置：`上传文件/一键生成短视频-豆包语音优化版.yml`
   - 大小：88KB
   - 编码：UTF-8

### 导入步骤
1. 在Dify中点击"导入"
2. 选择工作流文件
3. 点击"创建应用"

---

## 🛠️ 系统配置检查

### TTS服务配置
```bash
# 检查配置文件
cat /home/dify/tts_gateway/config.env

# 应包含以下配置：
# DOUBAO_API_KEY=your_api_key
# OSS_BASE_URL=http://8.148.70.18:9000
```

### CapCut API配置
```bash
# 检查CapCut API状态
cd /home/CapCutAPI-1.1.0
python app.py  # 确保运行在端口9000
```

---

## 🔍 故障诊断流程

### 步骤1: 检查基础服务
```bash
# TTS服务
curl http://localhost:3006/

# CapCut API服务  
curl http://8.148.70.18:9000/

# 网络连通性
ping 8.148.70.18
```

### 步骤2: 测试工作流组件
```bash
# 测试TTS API
curl -X POST http://localhost:3006/api/v1/tts \
  -H "Content-Type: application/json" \
  -d '{"text":"测试","voice_type":"zh_male_dongfanghaoran_moon_bigtts"}'

# 测试CapCut创建草稿
curl -X POST http://8.148.70.18:9000/create_draft \
  -H "Content-Type: application/json" \
  -d '{"width":1080,"height":1920}'
```

### 步骤3: 检查日志错误
```bash
# 查看TTS服务日志
cd /home/dify/tts_gateway
tail -f logs/app.log

# 查看CapCut API日志
cd /home/CapCutAPI-1.1.0
tail -f app.log
```

---

## ⚡ 一键修复脚本

运行快速诊断和修复：
```bash
cd /home/dify
./快速修复脚本.sh
```

脚本会自动检查：
- TTS服务状态并重启
- CapCut API连通性
- 网络连接状态
- 配置文件完整性
- 执行测试API调用

---

## 📞 紧急处理方案

### 完全重启服务
```bash
# 重启TTS服务
cd /home/dify/tts_gateway
pkill -f tts_gateway
sleep 2
./start.sh

# 重启CapCut API（如果需要）
cd /home/CapCutAPI-1.1.0
pkill -f app.py
sleep 2
python app.py &
```

### 清理和重置
```bash
# 清理TTS缓存
rm -f /home/dify/tts_gateway/audio_files/*.mp3

# 重新创建必要目录
mkdir -p /home/dify/tts_gateway/audio_files
mkdir -p /home/dify/tts_gateway/logs
```

---

## 🛠️ TTS工具参数配置错误详细解决方案

### 问题现象
```
Run failed: Failed to transform tool message: Request failed with status code 422 and {
  "detail": [
    {
      "type": "value_error",
      "loc": ["body", "voice_type"],
      "msg": "Value error, 不支持的音色类型: {{#start_1.tts_voice#|default(\"zh_male_dongfanghaoran_moon_bigtts\")}}"
    },
    {
      "type": "value_error", 
      "loc": ["body", "speed_ratio"],
      "msg": "Value error, 语速比例必须是有效的数字字符串",
      "input": "{{#start_1.tts_speed#|default(\"1.0\")}}"
    }
  ]
}
```

### 问题原因
Dify工作流中TTS工具的参数配置有问题，变量模板没有被正确解析，导致TTS服务接收到的是字面字符串而不是解析后的值。

### 解决步骤

#### 1. 修复voice_type参数
在工作流YAML文件中找到TTS工具节点，将voice_type参数配置修改为：
```yaml
voice_type:
  type: constant
  value: zh_male_dongfanghaoran_moon_bigtts
```

#### 2. 修复speed_ratio参数
将speed_ratio参数配置修改为：
```yaml
speed_ratio:
  type: constant
  value: '1.0'
```

#### 3. 验证修复
重新运行工作流，确认TTS工具节点不再报错。

### 支持的音色列表
- zh_male_dongfanghaoran_moon_bigtts （东方浩然-男声）
- zh_female_tianmei_moon_bigtts （甜美-女声）
- zh_male_zhigang_moon_bigtts （志刚-男声）
- zh_female_siyue_moon_bigtts （思悦-女声）
- zh_male_kaikai_moon_bigtts （开开-男声）
- zh_female_xiaoyu_moon_bigtts （小雨-女声）
- zh_male_zhengqi_moon_bigtts （正气-男声）
- zh_female_xiaomo_moon_bigtts （小沫-女声）
- zh_male_zhihao_moon_bigtts （志豪-男声）
- zh_female_xiaoxin_moon_bigtts （小欣-女声）

### 注意事项
- 如果需要用户可选择音色和语速，需要使用正确的变量引用方式
- 确保参数类型与TTS服务的API要求匹配
- speed_ratio必须是字符串格式的数字

---

## 📝 错误码对照表

| 错误信息 | 原因 | 解决方法 |
|---------|------|----------|
| `Connection refused` | 服务未启动 | 启动对应服务 |
| `Reached maximum retries` | API不可访问 | 检查服务状态和网络 |
| `媒体丢失` | 音频URL格式问题 | 已在工作流中修复 |
| `不支持的音色类型` | TTS参数配置错误 | 修改参数类型为constant |
| `语速比例必须是有效的数字字符串` | speed_ratio参数格式错误 | 使用constant类型和字符串值 |
| `创建应用失败` | 插件或配置问题 | 检查依赖和权限 |
| `401 Unauthorized` | API密钥错误 | 检查配置文件 |
| `403 Forbidden` | 权限不足 | 检查OSS配置 |

---

## 🎯 预防措施

### 定期检查
```bash
# 每日检查脚本（可加入crontab）
#!/bin/bash
curl -s http://localhost:3006/ > /dev/null || echo "TTS服务异常"
curl -s http://8.148.70.18:9000/ > /dev/null || echo "CapCut API异常"
```

### 监控建议
- 设置服务监控告警
- 定期备份配置文件
- 记录服务运行日志
- 定期清理临时文件

---

## 🎉 **最新解决方案：音频文件无法加载问题**

### 问题描述
- **现象**：工作流运行正常，但最终生成的草稿文件中没有音频
- **原因**：OSS签名问题导致音频URL不可访问（403 Forbidden）

### ✅ **完整解决方案**

#### 1. TTS服务fallback机制
```python
# 优先OSS上传，失败时自动fallback到本地文件
if not audio_url:
    local_filename = f"{int(time.time())}_{request.voice_type}.mp3"
    local_path = os.path.join("audio_files", local_filename)
    with open(local_path, "wb") as f:
        f.write(audio_data)
    audio_url = f"http://8.148.70.18:3006/audio/{local_filename}"
```

#### 2. 静态文件服务器
```python
# FastAPI静态文件支持
app.mount("/audio", StaticFiles(directory="audio_files"), name="audio")
```

#### 3. 智能URL处理
工作流中的"提取旁白URL"节点会：
- 检测URL类型（OSS vs 本地）
- 测试OSS URL可访问性
- 自动请求本地fallback（如果需要）
- 返回可用的音频URL

#### 4. 验证步骤
```bash
# 测试本地音频文件
curl -I "http://8.148.70.18:3006/audio/xxx.mp3"
# 应返回 HTTP/1.1 200 OK

# 测试CapCut处理
curl -X POST http://8.148.70.18:9000/add_audio \
  -d '{"audio_url": "http://8.148.70.18:3006/audio/xxx.mp3", ...}'
# 应返回 success: true
```

### 📈 **效果**
- ✅ 音频URL始终可访问
- ✅ CapCut正确处理MP3格式
- ✅ 最终视频包含音频
- ✅ 无需手动干预

---

**最后更新**: 2025-08-20  
**适用版本**: Dify v0.3.1, TTS Gateway v1.0, CapCut API v1.1.0 